# OPTIONAL -- only needed if you uncomment `weasyprint` in requirements.txt
# to enable one-click PDF resume export.
#
# This file uses the format Streamlit Community Cloud (and some other
# platforms) read to apt-get install system packages automatically. If
# you're running locally on Debian/Ubuntu, you can install the same
# packages yourself with:
#   sudo apt-get install -y libpango-1.0-0 libpangocairo-1.0-0 \
#       libgdk-pixbuf2.0-0 libcairo2 libffi-dev shared-mime-info fonts-liberation
#
# On Windows, WeasyPrint additionally requires the GTK3 runtime -- see
# https://doc.courtbouillon.org/weasyprint/stable/first_steps.html
#
# Without any of this, the app still works: resume export produces a
# styled HTML file that can be printed to PDF from any browser.

libpango-1.0-0
libpangocairo-1.0-0
libgdk-pixbuf2.0-0
libcairo2
libffi-dev
shared-mime-info
fonts-liberation
